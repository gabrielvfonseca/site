# gabfon.com — CLAUDE.md

Personal website and portfolio of Gabriel Fonseca (`hey@gabfon.com`), a Lisbon-based software developer, founder, and Computer Engineering student. Built with Next.js 16, TypeScript, and a Turborepo monorepo architecture.

## Project Structure

```
site-main/                         # Root monorepo (pnpm workspace + Turborepo)
├── apps/
│   └── web/                       # Next.js 16 app (name: "site", domain: gabfon.com)
│       ├── content/
│       │   ├── posts/             # MDX blog posts (3 posts)
│       │   └── projects/          # MDX project case studies (7 projects)
│       ├── src/
│       │   ├── app/               # App Router
│       │   │   ├── (site)/        # Route group: homepage, bio, contact, posts, projects, ama
│       │   │   ├── actions/       # Server Actions (contact, ama)
│       │   │   ├── api/           # API routes (activity, ama)
│       │   │   ├── layout.tsx     # Root layout (providers: Theme, Analytics, Query, Tooltip)
│       │   │   ├── sitemap.tsx, robots.ts, manifest.ts, rss.xml, llms.txt
│       │   │   └── opengraph-image.tsx, twitter-image.tsx, icon.tsx, apple-icon.tsx
│       │   ├── components/
│       │   │   ├── ui/            # shadcn/ui components (25 components)
│       │   │   ├── layout/        # Header, Footer
│       │   │   ├── mdx/           # Callout (MDX custom component)
│       │   │   ├── forms/         # ContactForm
│       │   │   ├── accessibility/ # SkipNav
│       │   │   ├── posts.tsx, posts-list.tsx
│       │   │   ├── projects.tsx, projects-list.tsx
│       │   │   ├── contribution-graph.tsx
│       │   │   └── theme-toggle.tsx
│       │   ├── config/            # env.ts (composed env vars)
│       │   ├── constants/         # config.ts, metadata.ts, cache.ts, colors.ts
│       │   ├── lib/               # utils.ts, content-index.ts, format-date.ts, og.tsx
│       │   ├── models/            # post.ts, project.ts (Zod schemas)
│       │   ├── schemas/           # contact.schema.ts
│       │   ├── exceptions/        # errors.ts
│       │   ├── providers/         # query-provider.tsx
│       │   ├── scripts/           # theme-init-script.tsx
│       │   ├── styles/            # main.css (Tailwind CSS v4)
│       │   ├── types/             # canvas-confetti.d.ts
│       │   └── utils/             # date.ts, post.ts, project.ts, structured-data.ts
│       ├── tests/
│       │   ├── e2e/               # Playwright tests
│       │   ├── unit/, integration/, load/, performance/, mock/, fixtures/, setup/
│       ├── public/                # Static assets
│       ├── .source/               # Auto-generated by fumadocs-mdx
│       ├── source.config.ts       # Fumadocs MDX collections config
│       ├── next.config.ts         # Next.js config with MDX, Sentry, Analyzer wrappers
│       ├── vitest.config.ts, playwright.config.ts, tsconfig.json
│
├── packages/                      # 16 workspace packages (@gabfon/*)
│   ├── analytics/                 # PostHog + Vercel Analytics
│   ├── database/                  # Neon / PostgreSQL client
│   ├── design-system/             # shadcn/ui source of truth (30+ components), Radix, Tailwind
│   ├── email/                     # Email templates (contact notifications)
│   ├── github/                    # GitHub API client (contribution graph, stats)
│   ├── next-config/               # Shared Next.js config presets
│   ├── observability/             # Sentry + Logtail
│   ├── rate-limit/                # Upstash Redis / Vercel KV rate limiting
│   ├── security/                  # Arcjet + Nosecone middleware
│   ├── seo/                       # Metadata, viewport, JSON-LD helpers
│   ├── spotify/                   # Spotify API client (now playing, top tracks)
│   ├── strava/                    # Strava API client (activities, runs)
│   ├── testing/                   # Shared test setup (mocks for ResizeObserver, matchMedia, etc.)
│   ├── typescript-config/         # Shared tsconfig presets
│   ├── wakatime/                  # WakaTime API client (coding hours)
│   └── x/                         # X (Twitter) API client (latest post)
│
├── scripts/                       # CLI scripts (security scan, changelog, etc.)
├── types/                         # Global ambient type declarations
├── docs/                          # Project documentation
├── skills/                        # AI agent skills (18 skill directories)
├── .claude/                       # Claude Code config (CLAUDEMODEL, workflows, agents)
├── .github/                       # CI workflows, templates, policies
├── .husky/                        # Git hooks (commitlint, lint-staged, gitleaks)
└── .changeset/                    # Changesets config
```

## Tech Stack

| Layer | Technology |
|-------|-----------|
| **Framework** | Next.js 16.2.1 (App Router) with React 19.2.1 |
| **Monorepo** | Turborepo 2.9 + pnpm 9.12.3 + Bun |
| **Language** | TypeScript 5.9 (strict mode) |
| **Styling** | Tailwind CSS v4 + `tw-animate-css` + OKLCH color tokens |
| **UI Library** | shadcn/ui (base-nova) + Radix UI + @base-ui/react |
| **Content** | Fumadocs MDX (`.mdx` files → typed collections at build time — NO database) |
| **Client Data** | @tanstack/react-query |
| **Validation** | Zod + @t3-oss/env-nextjs |
| **Linting/Format** | Biome 2.2 via `ultracite` |
| **Testing** | Vitest + Testing Library (unit/integration), Playwright (E2E) |
| **Security** | Arcjet + Nosecone + Gitleaks + Snyk |
| **Observability** | Sentry + PostHog + Logtail + Vercel Analytics |
| **Deployment** | Vercel (Bun runtime) + optional Docker |

## Commands

Run from repo root unless prefixed with `apps/web/`.

```bash
pnpm dev              # Start all dev servers via Turborepo
pnpm build            # Build all packages + app
pnpm test             # Run all tests (vitest run --run)
pnpm test:coverage    # With coverage report
pnpm typecheck        # TypeScript validation
pnpm lint             # ultracite check (Biome)
pnpm format           # ultracite fix (Biome auto-fix)
pnpm clean            # git clean -xdf node_modules

# App-specific
pnpm --filter site dev            # Next.js dev server
pnpm --filter site test           # vitest run
pnpm --filter site test:e2e       # playwright test
pnpm --filter site test:watch     # vitest watch
pnpm --filter site analyze        # next analyze (bundle analysis)
pnpm --filter site build          # next build

# Release
pnpm changeset          # Create a changeset
pnpm version            # Apply changesets
pnpm release            # Publish packages

# Maintenance
pnpm bump-deps          # Bump all deps via npm-check-updates
pnpm bump-ui            # Re-add all shadcn/ui components
```

## Coding Conventions

### General
- **Files**: kebab-case (enforced by Biome)
- **Components**: PascalCase named function exports
- **Utilities**: camelCase
- **Constants**: UPPER_SNAKE_CASE or PascalCase
- **Props interfaces**: `ComponentNameProps` (defined locally, same file)
- **All functions, components, interfaces, exports require JSDoc** with `@param` and `@returns`

### Imports
- Third-party imports first, then internal absolute imports
- Absolute path alias: `@/` → `./src/`
- Workspace packages: `@gabfon/*`
- No barrel file restrictions (Biome rules `noBarrelFile` and `noReExportAll` are disabled)

### Components
- **React Server Components by default** — only add `'use client'` for interactivity
- Composition pattern: data-fetching parent (`Posts`, `Projects`) → rendering child (`PostsList`, `ProjectsList`)
- Accessibility: semantic HTML, `aria-label`, `aria-labelledby`, skip-nav, focus management

### Types
- Explicit type annotations on parameters and return types
- `interface` for props, `type` for unions/aliases
- Zod schemas for runtime validation with `z.infer<typeof schema>` inference
- `readonly` modifier on props interface members

### Example Component
```tsx
interface GreetingProps {
  readonly name: string;
  readonly variant?: 'primary' | 'secondary';
}

export function Greeting({ name, variant = 'primary' }: GreetingProps): JSX.Element {
  return <h2 className={cn('font-semibold', variant === 'primary' && 'text-lg')}>{name}</h2>;
}
```

## Page + API Route Structure

### Public Routes (under `(site)` route group)
| Path | Description |
|------|-------------|
| `/` | Homepage (about, activity graph, projects, posts) |
| `/bio` | Bio page |
| `/contact` | Contact form |
| `/posts` | Blog listing |
| `/posts/[slug]` | Individual post |
| `/projects` | Projects listing |
| `/projects/[slug]` | Project case study |
| `/ama` | Ask Me Anything |

### API Routes
| Path | Description |
|------|-------------|
| `/api/activity` | GitHub/Spotify/Strava contribution data |
| `/api/ama` | AMA questions (read/write) |

### Server Actions
| Path | Description |
|------|-------------|
| `actions/contact/actions.ts` | Contact form submission |
| `actions/ama/actions.ts` | AMA question submission |

### Metadata Routes (auto-generated)
`/sitemap.tsx`, `/robots.ts`, `/manifest.ts`, `/icon.tsx`, `/apple-icon.tsx`, `/opengraph-image.tsx`, `/twitter-image.tsx`, `/llms.txt`, `/llms.md`, `/llms-full.txt`, `/rss.xml`

## Content Management (Fumadocs MDX)

All content is file-based MDX in `apps/web/content/`. No database.

- **Source config**: `apps/web/source.config.ts` defines `posts` and `projects` collections with Zod schemas from `@/models/post` and `@/models/project`
- **Build-time compilation**: `fumadocs-mdx` generates typed output to `apps/web/.source/`
- **Access functions**: `@/lib/content-index` exposes `getPosts()`, `getProjects()`, `getPostEntry(slug)`, `getProjectEntry(slug)`
- **MDX rendering**: Entry `.body` is a React component rendered as `<MdxContent />` inside a `.prose` container
- **Syntax highlighting**: Shiki with dual theme (`github-light` / `github-dark`)

## Environment Variables

All env vars are validated at startup via `@t3-oss/env-nextjs` + Zod in `apps/web/src/config/env.ts`. Each `@gabfon/*` package exposes a `keys()` factory composed via `createEnv({ extends: [...] })`.

### Required
`DATABASE_URL`, `ARCJET_KEY`, `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`, `SENTRY_ORG`, `SENTRY_PROJECT`, `NEXT_PUBLIC_SENTRY_DSN`, `NEXT_PUBLIC_WEB_URL`, `NEXT_PUBLIC_POSTHOG_KEY`, `NEXT_PUBLIC_POSTHOG_HOST`, `LOGTAIL_SOURCE_TOKEN`

### Optional (with defaults)
`NEXT_PUBLIC_X_URL`, `NEXT_PUBLIC_LINKEDIN_URL`, `NEXT_PUBLIC_GITHUB_URL`, `NEXT_PUBLIC_SCHEDULE_URL`, `NEXT_PUBLIC_EMAIL`, `ANALYZE`, `FLAGS_SECRET`

## Security Middleware Chain

Applied via middleware (in order):
1. **Rate Limiter** (`@gabfon/rate-limit`) — cheapest rejection first
2. **Nosecone** (`@gabfon/security`) — security headers (CSP, HSTS, etc.)
3. **Arcjet** (`@arcjet/next`) — bot detection + shield protection

Plus: Gitleaks pre-commit hook, Snyk dependency scanning, pnpm overrides for vulnerability patches.

## Styling (Tailwind CSS v4)

- **Entry**: `apps/web/src/styles/main.css` — `@import "tailwindcss"`, design system globals, `tw-animate-css`
- **Dark mode**: `.dark` class variant (`@custom-variant dark (&:is(.dark *))`)
- **Colors**: OKLCH color space with CSS custom properties (`--background`, `--foreground`, `--primary`, etc.)
- **Component utility**: `cn()` function (`clsx` + `tailwind-merge`)
- **No CSS modules** — all styling via inline Tailwind utilities

## Key Patterns

1. **Workspace package isolation**: Every external integration (GitHub, Spotify, Strava, analytics, security) is a standalone `@gabfon/*` package with its own env `keys()` factory
2. **Env composition**: `apps/web/src/config/env.ts` composes all package env schemas via `createEnv({ extends: [...] })`
3. **Server-first**: Minimal client JS — only `theme-toggle`, `ContactForm`, `ContributionGraph`, `QueryProvider` are client components
4. **Composition + delegation**: `Posts` calls `getPosts()` → delegates rendering to `PostsList`; `Projects` → `ProjectsList`
5. **Conventional commits**: `feat:`, `fix:`, `docs:`, `chore:`, `refactor:`, `test:`, `style:`, `revert:`, `ci:`, `perf:`, `build:` enforced by commitlint + husky
6. **No barrel files**: Biome explicitly disables `noBarrelFile` and `noReExportAll` rules to allow barrel exports where useful
7. **JSDoc everywhere**: Every export — functions, components, interfaces — has a JSDoc comment

## Testing

| Type | Framework | Location |
|------|-----------|----------|
| Unit | Vitest + Testing Library + jsdom | `src/**/*.test.{ts,tsx}`, `tests/unit/**/*` |
| Integration | Vitest | `tests/integration/**/*` |
| E2E | Playwright (Chromium, Firefox, WebKit, Mobile Chrome) | `tests/e2e/**/*` |
| Load | k6 (config present) | `tests/load/**/*` |
| Coverage | Vitest with 80% threshold | `pnpm test:coverage` |

- Vitest config in `apps/web/vitest.config.ts` — jsdom environment, alias for `@/` and all `@gabfon/*` packages
- Playwright config in `apps/web/playwright.config.ts` — retries (2 in CI), HTML reporter, screenshot-only-on-failure
- Shared test setup in `packages/testing/` — mocks for `ResizeObserver`, `matchMedia`, `IntersectionObserver`

## Available AI Agent Skills

Skills are loaded automatically when matching tasks arise:
- `code-review` — PR/code review
- `playwright` — Browser automation, E2E testing
- `frontend-design` — Creative UI/UX, distinctive aesthetics
- `next-best-practices` — Next.js 16 architecture, App Router, performance
- `find-docs` — Fetching up-to-date library/framework documentation
- `web-animation-design` — CSS/JS animations, easing, motion
- `vercel-react-best-practices` — React/Next.js performance optimization
- `workflow` — Durable, resumable workflows
- `deploy-to-vercel` — Deployment automation
- `skill-creator` — Creating/updating agent skills
