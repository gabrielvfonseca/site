{
  "buildCommand": "bun install && bun run build --filter=site",
  "outputDirectory": "apps/www/.next",
  "installCommand": "bun install --frozen-lockfile",
  "framework": "nextjs",
  "crons": [],
  "env": {}
}
