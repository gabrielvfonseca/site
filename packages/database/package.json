{
  "name": "@gabfon/database",
  "version": "0.0.2",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./keys": "./src/keys.ts"
  },
  "scripts": {
    "build": "prisma generate --no-hints --schema=./prisma/schema.prisma && tsc --noEmit --emitDeclarationOnly false",
    "postinstall": "prisma generate --no-hints --schema=./prisma/schema.prisma",
    "test": "echo 'No tests specified'",
    "analyze": "prisma generate --no-hints --schema=./prisma/schema.prisma",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.1",
    "@prisma/adapter-neon": "6.16.1",
    "@prisma/client": "6.16.1",
    "@t3-oss/env-nextjs": "^0.13.8",
    "server-only": "^0.0.1",
    "undici": "^7.15.0",
    "ws": "^8.18.3",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@gabfon/typescript-config": "workspace:*",
    "@types/node": "24.3.1",
    "@types/ws": "^8.18.1",
    "bufferutil": "^4.0.9",
    "prisma": "6.16.1",
    "typescript": "^5.9.2"
  }
}
