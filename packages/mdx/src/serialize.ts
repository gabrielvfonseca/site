/**
 * Processes MDX content string (simplified version)
 * @param content - The MDX content string to process
 * @returns Promise<string>
 */
export function serializeMdxContent(content: string): string {
  // For now, just return the content as-is
  // In the future, this could be enhanced with proper MDX processing
  return content;
}
